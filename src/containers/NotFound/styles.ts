import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  container: {
    height: "80vh",
    display: "grid",
    textAlign: "center",
    placeItems: "center",
  },
});

export default useStyles;
