import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  form: {
    marginTop: "20px",
  },
  submit_button: {
    padding: "8px 0",
    marginTop: "16px",
  },
});

export default useStyles;
